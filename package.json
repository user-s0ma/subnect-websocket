{
  "name": "subnect-websocket",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "prisma generate && wrangler deploy",
    "dev": "prisma generate && wrangler dev",
    "start": "prisma generate && wrangler dev",
    "test": "vitest",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "prisma": "5.20.0",
    "@prisma/client": "5.20.0",
    "@tidbcloud/prisma-adapter": "5.20.0",
    "@tidbcloud/serverless": "0.2.0",
    "next-auth": "5.0.0-beta.25",
    "@auth/prisma-adapter": "2.7.2"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "@cloudflare/workers-types": "^4.20240821.1",
    "@types/node": "22.8.4",
    "typescript": "^5.5.2",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  }
}
